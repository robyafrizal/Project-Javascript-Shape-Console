const rectangleWidth = 100;
const rectangleLength = 20;

const rectangleArea = rectangleWidth * rectangleLength;

console.log(
  `Luas dari Persegi dengan lebar ${rectangleWidth} dan panjang ${rectangleLength} adalah ${rectangleArea}`
);
